apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ecomm-web.fullname" . }}-test-connection"
  labels:
    {{- include "ecomm-web.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ecomm-web.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
