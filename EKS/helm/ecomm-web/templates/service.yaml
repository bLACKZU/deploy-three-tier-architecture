apiVersion: v1
kind: Service
metadata:
  name: {{ include "ecomm-web.fullname" . }}
  labels:
    {{- include "ecomm-web.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ecomm-web.selectorLabels" . | nindent 4 }}
